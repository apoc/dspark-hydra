# Model / target facts (§1).  Validate with scripts/validate_config.py.
target:
  repo_id: Qwen/Qwen3.6-35B-A3B
  model_type: qwen3_5_moe
  hidden_size: 2048
  vocab_size: 248320
  num_hidden_layers: 40
  full_attention_interval: 4                  # full-attn layers: 3,7,11,…,39
  num_experts: 256
  num_experts_per_tok: 8                      # + 1 shared
  moe_intermediate_size: 512
  shared_expert_intermediate_size: 512
  mtp_num_hidden_layers: 1                    # native MTP-1 baseline (B0)
